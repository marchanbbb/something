解决粘包问题的常用方案有以下 3 种：

设置固定大小的消息长度，如果长度不足则使用空字符弥补，它的缺点比较明显，比较消耗网络流量，因此不建议使用；
应用：消息内容可预知，大小变化不大

使用分隔符来确定消息的边界，从而避免粘包和半包问题的产生；
应用：包体长度不定，对流量要求较严格的场景适合

将消息分为消息头和消息体，在头部中保存有当前整个消息的长度，只有在读取到足够长度的消息之后才算是读到了一个完整的消息。
应用：包体长度不定，对流量要求没那么严格的场景适合